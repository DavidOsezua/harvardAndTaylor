import Banner from "../components/Banner";

const Projects = () => {
  return (
    <div className="min-h-screen ">
      <Banner
        label={"Portfolio"}
        title={"Selected Investments"}
        subtitle="A concentrated portfolio of carefully selected positions, each representing a conviction-driven allocation in UK real assets."
      />
    </div>
  );
};

export default Projects;
